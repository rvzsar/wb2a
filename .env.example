# API key upstream (W&B Inference)
WANDB_API_KEY=your_wandb_api_key_here

# Ключ прокси: если задан, клиенты обязаны слать его в Authorization: Bearer
# (upstream всегда получает WANDB_API_KEY, анонимный доступ закрыт)
PROXY_API_KEY=

# Порт (локальный запуск)
PORT=8000
